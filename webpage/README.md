## Local Set Up
- clone project

### Frontend
- navigate to frontend directory. 
- run `npm i && npm run dev`

### Using Docker-Compose Instead
- Open Docker Desktop, Rancher Desktop, Podman, or any other platform alternatives.
- Run `docker-compose up`. Might differ based on platform used.